/goal Implement Reflex from docs/SPEC.md as a local rust-based Codex plugin. Use docs/STYLE.md to guide the development. Build the plugin package with hooks, bundled MCP server, skill instructions, SQLite storage, redaction, episode lifecycle, lesson retrieval/injection, CLI inspection commands, tests, and a README.md with a convincing hook, example, and installation instructions. Work in checkpoints: scaffold package; implement storage/redaction; implement hooks; implement MCP tools; implement lesson matching/injection; implement CLI; add fixture-based tests and demos; document install/trust steps. Validate with automated tests and the wrong-cwd, package-manager, explicit-registration, and secrets-redaction demos. Stop only when the plugin installs locally, hooks run, MCP tools are visible to Codex, lessons can be registered, and relevant hints are injected without storing secrets.
